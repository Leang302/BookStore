$(document).ready(function () {
    $(".slider").slick({
        autoplay: true,
        autoplaySpeed: 2000, // Change slide every 3 seconds
        arrows: false, // Optionally, hide navigation arrows
        dots: false, // Optionally, add navigation dots
    });
});
